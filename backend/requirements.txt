# FastAPI 后端依赖
# 最小外部依赖版：bcrypt 已系统安装，fastapi/uvicorn 已全局安装
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
bcrypt>=4.0.0
python-multipart>=0.0.9
pydantic>=2.0.0

# WebSocket（fastapi 内置 websockets 支持）
websockets>=10.0

# 可选（网络可用时安装）
# python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4
# aiosqlite==0.20.0
# pytest==8.3.4
# pytest-asyncio==0.25.0
# httpx==0.28.1
